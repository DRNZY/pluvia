[package]
name = "pluvia-core"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4"
encoding_rs = "0.8"
libc = "0.2"
thiserror.workspace = true
zip = "2.2"
zbus = { version = "5.0", default-features = false, features = ["tokio", "blocking-api"] }

[dev-dependencies]
tempfile = "3.12"

